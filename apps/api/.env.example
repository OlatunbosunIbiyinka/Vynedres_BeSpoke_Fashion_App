DATABASE_URL="postgresql://vynedres:vynedres@localhost:5432/vynedres?schema=public"
PORT=3001
HOST=0.0.0.0
NODE_ENV=development
LOG_LEVEL=info
CORS_ORIGIN=http://localhost:3000

# Required for studio login (JWT signing). Use a long random string in production.
JWT_SECRET="dev-only-change-me-in-production"
JWT_EXPIRES_IN="7d"

# Optional: freer natural-language routing for the studio assistant.
# Metrics always come from analytics tools (never invented by the model).
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
